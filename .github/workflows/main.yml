name: test
on: workflow_dispatch
jobs:
  job:
    runs-on: ubuntu-latest
    steps: 
      - name: test2
        run: echo " hello"   
      - name: print sys information
        run: date 
      - name: system details
      - run: uname -a
      - run: free -h
      - run: du -hT
      
      
  
