name: test-env
on: push
env:
  APP_ENV: "dev"
jobs:
 test-env:
  env:
   APP_ENV: "TEST"
  runs-on: ubuntu-latest
  steps:
    - name: connecting to env
      run: echo "app connecting to $ APP_ENV"
      
  
    
 
