name:
on:
 push:
  branches: 
   - main
   - test
jobs:
   test:
    runs-on: ubuntu-latest
    steps:
       - name:  run if main branch push
         if: github.ref=='ref/heads/main'
         run: echo "this is main"
       - name: run this step
         run: echo"this is main"
